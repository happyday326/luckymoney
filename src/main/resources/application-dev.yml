server:
  port: 8081
  servlet:
    context-path: /luckymoney

limit:
  minMoney: 0.01
  maxMoney: 9999
  description: minimum ${limit.minMoney} dollar, maximum ${limit.maxMoney} dollar

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/luckymoney?serverTimezone=UTC
    username: root
    password: 19931127
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
